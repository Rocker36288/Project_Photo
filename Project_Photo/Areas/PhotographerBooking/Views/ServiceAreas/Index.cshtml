@model IEnumerable<Project_Photo.Models.ServiceArea>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AdditionalFee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Photographer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdditionalFee)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Photographer.StudioName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ServiceAreaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ServiceAreaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ServiceAreaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
